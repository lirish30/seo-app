interface ScoreCardProps {
    title: string;
    score: number;
    description?: string;
}
export declare function ScoreCard({ title, score, description }: ScoreCardProps): import("react/jsx-runtime").JSX.Element;
export {};
